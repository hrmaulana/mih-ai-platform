# Env & secrets
.env
.env.local
.env.prod
secrets/
deploy/vps/cloudflare.ini

# Compose override lokal (mis. ekspos port backend hanya ke Tailscale)
docker-compose.override.yml

# Node
node_modules/
dist/
*.log

# Python
__pycache__/
*.pyc
.venv/

# Data (uploads & raw files — tidak ikut git; hanya di root, bukan source src/data)
/data/
.test-data/

# OS
.DS_Store
Thumbs.db

# Referensi landing page asli (save-page dari tokogd.com) — tidak ikut git
PMP Portal.html
PMP Portal_files/

# Artifak lokal tooling (bukan bagian repo)
.commandcode/
*.tar.gz
